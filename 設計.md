# RichText ライブラリ設計資料

## 1. 概要

多言語・装飾対応のリッチテキストレンダリングライブラリ。minikin によるテキストレイアウト、glyphware（FreeType）によるグリフのラスタライズ、自前のマスク合成による描画を組み合わせて、高品質なテキスト描画を実現する。

### 1.1 主要機能

- **1行テキストレンダリング**: シンプルな1行テキストの高速描画
- **パラグラフレンダリング**: 矩形領域内での複数行テキスト描画（左揃え・中央・右揃え対応）
- **多言語対応**: 複数言語の混在、RTL（右から左）テキスト対応
- **フォントフォールバック**: 複数フォントを束ねて文字カバレッジを補完
- **絵文字対応**: カラー絵文字を含むテキスト描画
- **スタイル指定**: フォント、サイズ、色、スペーシングなどのスタイル制御
- **装飾描画**: ストローク（縁取り）と塗り（Fill）を複数回重ねる装飾処理
- **インラインタグ**: HTMLライクなタグでテキスト途中のスタイル変更
- **CJK ロケール対応**: 同一コードポイントでもロケール別に異なる字形を描画
- **縦組み**: JLReq 水準の日本語縦書き（約物の詰め・禁則・追い込み／追い出し・ルビ・縦中横・圏点・割注・字取り・段組・ページ流し込み）
- **PDF 出力**: 同一の組版結果から画面と PDF を出す（任意ターゲット）

---

## 2. 使用ライブラリと役割

| ライブラリ | 役割 | 管理方法 |
|-----------|------|---------|
| **minikin** | テキストレイアウトエンジン（行分割、グリフ配置座標計算） | git submodule (`ext/minikin`) |
| **glyphware** | フォント管理・グリフのラスタライズ（FreeType ラッパ） | 外部ツリー（`GLYPHWARE_DIR`） |
| **FreeType** | フォント読み込み、アウトライン取得、ラスタライズ（glyphware 経由） | vcpkg |
| **harfbuzz** | 複雑なテキスト整形（minikin 内部で使用） | minikin に内包 |
| **ICU** | Unicode 処理、行分割ルール（minikin 内部で使用） | minikin に内包 |
| **zlib / libpng** | PNG 出力（サンプル用） | vcpkg |

### 2.1 データフロー

```
テキスト入力 (UTF-16)
        ↓
[TagParser] タグ解析
  - HTMLライクタグをパース
  - StyleRun 配列 + Appearance 配列に変換
        ↓
[minikin] テキストレイアウト
  - フォント選択（フォールバック処理）
  - MeasuredText で文字幅計算
  - breakIntoLines で行分割
  - グリフID・配置座標の算出
        ↓
[glyphware / FreeType] グリフのラスタライズ
  - 変形（サイズ・斜体・幅・サブピクセル位置）をアウトラインに焼き込み
  - 8bit カバレッジマスクを生成（縁取りは FT_Stroker）
  - カラー絵文字はビットマップ / COLR レイヤーとして取得
        ↓
[Raster] マスク合成
  - Appearance に従い 影 → 縁取り → 塗り の順で色を乗せる
        ↓
出力ビットマップ (ARGB)
```

---

## 3. レイヤー構造

```
タグ解析層           src/TagParser.cpp      HTMLライクなタグ→StyleRun/Appearance変換
描画層               src/TextRenderer.cpp   描画統合インタフェース
                     src/GlyphRenderer.cpp  グリフ単位描画・キャッシング
                     src/pdf/PdfWriter.cpp  PDF 出力（任意ターゲット richtext_pdf）
レイアウト層（横組み） src/StyledLayout.cpp    タグ付きテキストのレイアウト（タグ解析+行分割+セグメント構築）
                     src/ParagraphLayout.cpp 複数行レイアウト（行分割）
                     src/TextLayout.cpp      1行レイアウト（minikin::Layout）
レイアウト層（縦組み） src/vertical/BlockLayout.cpp     段組・連結コンテナ流し込み・ページ分割
                     src/vertical/VerticalParagraphLayout.cpp 縦組み段落（行分割＋配置）
                     src/vertical/LineBreaker.cpp     自前行分割（Greedy / Knuth-Plass）
                     src/vertical/LineItemBuilder.cpp クラスタ列→Box/Glue/Penalty 列
                     src/vertical/SpacingTable.cpp    JLReq 表3 のアキ量
                     src/vertical/CharClass.cpp       JLReq 文字クラス・禁則
                     src/vertical/VerticalLayout.cpp  縦1行レイアウト（ベタ組み）
                     src/vertical/VerticalShaper.cpp  縦組みシェイピング（HarfBuzz TTB 直叩き）
                     src/vertical/WritingMode.cpp     書字方向・座標系・向き判定
テクスチャ管理層     src/TextureAtlas.cpp    グリフのテクスチャアトラス管理
スタイル管理層       src/TextStyle.cpp       minikin::MinikinPaint 設定
                     src/Appearance.cpp      DrawStyle（塗り/ストローク）
フォント管理層       src/FontFace.cpp        FreeType ↔ minikin 橋渡し
                     src/FontManager.cpp     フォント登録・シングルトン管理
```

---

## 4. 主要クラス

### 4.1 FontManager

フォントの登録・管理を行うシングルトンクラス。

#### フォントローダー

ライブラリ自体はファイルシステムへの直接アクセスを行わない。利用側がフォントデータの読み込み関数を登録する。

- `setFontDataLoader(loader)` — バッファ方式ローダーを登録。`FontDataBuffer`（`shared_ptr<vector<uint8_t>>`）を返す関数を設定する。**優先的に使用される。**
- `setFontStreamLoader(loader)` — FreeType ストリーム方式ローダーを登録。`FT_Stream` を返す関数を設定する。バッファローダーが未登録または読み込みに失敗した場合のフォールバックとして使用される。

フォント登録時（`registerFont` / `registerVariableFont`）に、登録されたローダーが呼び出されてフォントデータを取得する。

```cpp
// 使用例: ファイルシステムから読み込むバッファローダー
fm.setFontDataLoader([](const std::string& name) -> richtext::FontDataBuffer {
    std::ifstream file("./data/" + name, std::ios::binary | std::ios::ate);
    if (!file) return nullptr;
    auto size = file.tellg();
    file.seekg(0, std::ios::beg);
    auto buffer = std::make_shared<std::vector<uint8_t>>(size);
    if (!file.read(reinterpret_cast<char*>(buffer->data()), size))
        return nullptr;
    return buffer;
});

// フォント登録（ローダーにはファイル名が渡される）
fm.registerFont("NotoSans-Regular.ttf", "sans");
```

#### ローダー方式の選択指針

| 方式 | 用途 | 特徴 |
|------|------|------|
| **バッファ方式** | 通常利用、アーカイブ内フォント | フォントデータ全体をメモリに保持。FreeType は `FT_OPEN_MEMORY` でバッファを直接参照（内部コピーなし）。minikin / harfbuzz もバッファに直接アクセス可能 |
| **ストリーム方式** | 巨大フォントの遅延読み込み | FreeType の `FT_OPEN_STREAM` を使用。必要なテーブルのみを読み込むため初期メモリ消費が少ない。ただし `GetFontData()` が nullptr を返すため、minikin / harfbuzz のテーブル直接参照は FreeType 経由になる |

#### フォント管理 API

- `registerFont(fileName, name, index)` — フォント登録。`fileName` はローダーに渡される識別子、`name` は `createCollection` 等で使用する登録名
- `registerVariableFont(fileName, name, weight, italic, index)` — バリアブルフォントをウェイト・スタイル指定で登録
- `createCollection(names)` — フォント名リスト（優先度順）からフォールバックコレクション作成
- `registerLocale(locale)` — ロケール文字列を登録しIDを返す（例: `"ja_JP"`, `"zh_Hant"`）

#### フォント名解決ルール

`getFont` / `createCollection` でフォントを引く際の照合順序:

1. **登録名で完全一致** — `registerFont` / `registerVariableFont` で指定した `name` と一致するか
2. **family 名で一致** — フォントファイル内の name テーブルから取得した family 名（`FT_Face::family_name`、例: `"Noto Sans JP"`）と一致するか
3. **"family Style" 連結名で一致** — family 名 + 半角スペース + style 名（`FT_Face::style_name`、例: `"Noto Sans JP Bold"`）と一致するか

`createCollection` で family 単独一致したケースでは、同じ family 名を持つ複数の登録エントリ（異なる weight / style）が 1 つの `minikin::FontFamily` に集約される。これにより `registerFont` で個別に登録した Regular / Bold / Italic を `createCollection({"Noto Sans JP"})` で一括参照できる。

### 4.2 FontFace

`minikin::MinikinFont` を継承。FreeType フォントフェイスをラップ。

2種類のコンストラクタを持ち、FontManager のローダーから呼び出される:

- `FontFace(name, shared_ptr<FontBackendFace>, index)` — バックエンドが開いた face を包む

主要メソッド:

- `getBackendFace()` — バックエンド face（マスク・アウトライン・カラーレイヤー取得用）
- `getGlyphBitmap()` — カラー絵文字用ビットマップ取得
- `isColorGlyph()` — カラー絵文字かどうか判定
- `isVariableFont()` — バリアブルフォントかどうか判定
- `setVariations()` — バリエーション軸（wght, wdth 等）の値を設定
- `getFontName()` — 登録時に渡された名前
- `getFamilyName()` — フォントファイル内の family 名（`FT_Face::family_name`、例: `"Noto Sans JP"`）。取得できなければ空文字列
- `getStyleName()` — フォントファイル内の style 名（`FT_Face::style_name`、例: `"Regular"`）。取得できなければ空文字列

### 4.3 TextStyle / DrawStyle / Appearance

**TextStyle**: フォント、サイズ、ウェイト、イタリック、字間、ロケールなどの論理スタイル。`applyTo()` で `minikin::MinikinPaint` に変換。

**DrawStyle**: 個別の描画操作（Fill または Stroke）。色、オフセット、ストローク幅など。

**Appearance**: 複数の DrawStyle を組み合わせた描画外観。描画順序は配列順で、**影 Fill → Stroke → 通常 Fill** の順に配置する。`set*` / `add*` メソッドが自動的にレイヤー位置を管理する。

### 4.4 TextLayout

1行テキストのレイアウト。`minikin::Layout` を使用。

### 4.5 ParagraphLayout

複数行テキストのレイアウト。`minikin::MeasuredText` + `breakIntoLines` を使用。

- `getRunWidth(start, end)` — MeasuredText の文字幅を合算。TextRenderer でのセグメント配置に使用し、行分割とレンダリングの幅を一致させる。

### 4.6 GlyphRenderer

グリフ単位の描画。バックエンドに作らせた 8bit カバレッジマスク、またはビットマップを `RenderTarget` へ合成する。

- 変形（サイズ・フェイク幅・シアー・上下反転・ユーザ変換・サブピクセル位置）は 1 つのアフィン行列に畳み込み、マスク生成時にアウトラインへ焼き込む。後段でスケールしないので拡大縮小や斜体で品質が落ちない。
- フェイクイタリック: `FontFakery::isFakeItalic()` が true の場合、変形行列に X シアー（`skewX = -0.25`）を合成する。
- フェイクボールド: `FontFakery::isFakeBold()` が true の場合、塗りマスクに同色の縁取りマスク（フォントサイズの 1/24 幅）を重ねて太字をシミュレートする。
- マスクキャッシュのキーは「フォント・グリフID・サイズ・フォント幅・ストローク幅・変形の 2x2 成分・サブピクセル位相（1/4px）」。平行移動の整数部だけがキャッシュ間で共有される。

### 4.7 StyledLayout

タグ付きテキストのレイアウト処理を分離したクラス。`TextRenderer::drawStyledText()` が内部で行っていたタグ解析・行分割・セグメント構築を事前計算し、結果を保持する。

**主要メソッド:**

- `layout(text, maxWidth, maxHeight, hAlign, vAlign, styles, appearances, lineSpacing)` — レイアウト実行

**取得可能な情報:**

- `getParsed()` — `TagParser::ParseResult`（スパン情報を含む）
- `getParagraphLayout()` — `ParagraphLayout`（行情報）
- `getLineLayouts()` — 行ごとのセグメントレイアウト配列（`SegmentLayout` に TextLayout・measuredWidth・spanIdx 等を格納）
- `getTotalGlyphCount()` — 総グリフ数
- `getTotalCharCount()` — 総文字数（ユニーク charIndex 数）
- `getHAlign()` / `getVAlign()` — アライン設定
- `getMaxWidth()` / `getMaxHeight()` — レイアウト領域サイズ

**用途:**

1. レイアウトを事前計算し、`TextRenderer::drawStyledLayout()` で描画（maxGlyphs による逐次表示対応）
2. `TextureAtlas::addStyledLayout()` でグリフをアトラスに登録し、`getCopyRects()` でコピー矩形を生成
3. `drawStyledText()` は内部で `StyledLayout` を生成して `drawStyledLayout()` を呼び出す

### 4.8 TextRenderer

描画の統合インタフェース。

- `drawText()` — 1行テキスト描画
- `drawParagraph()` — パラグラフ描画
- `drawStyledText()` — TagParser 結果を受けてスタイル付きテキスト描画（内部で StyledLayout を使用）
- `drawStyledLayout()` — 事前計算済み StyledLayout の描画（maxGlyphs による逐次表示対応）

### 4.9 TextureAtlas

グリフを事前レンダリングしてテクスチャに格納し、コピー矩形配列で表示順に転送する。

キャッシュキーは `font + glyphId + fontSize + fontWidth + fakery + appearance` の全情報で構成され、同一グリフでもスタイルや外観が異なれば別エントリとして管理される。

- `addLayout()` — TextLayout のグリフをアトラスに追加
- `addParagraphLayout()` — ParagraphLayout のグリフをアトラスに追加
- `addStyledLayout()` — StyledLayout のグリフをアトラスに追加（各セグメントのスタイル・外観を自動参照）
- `commit()` — アトラスをテクスチャに書き込み
- `getCopyRects()` — TextLayout / ParagraphLayout / StyledLayout から表示用コピー矩形配列を生成（maxGlyphs 対応）。TextLayout / ParagraphLayout 版は appearance パラメータが必要

### 4.10 TagParser

HTMLライクなインラインタグを解析し、`StyleRun` + `Appearance` の配列に変換。

対応タグ:
- `<font size=... weight=... spacing=... width=... face=...>` フォント変更（`face` で名前付きコレクションを参照可）
- `<color value=0xAARRGGBB [add]>` / `<color r=... g=... b=... a=... [add]>` 色変更
- `<b>` / `<strong>` 太字
- `<i>` / `<em>` 斜体
- `<u>` 下線
- `<s>` / `<strike>` / `<del>` 取り消し線
- `<ruby text='...'>` ルビ
- `<sup>` 上付き / `<sub>` 下付き
- `<outline color=... width=... [add]>` 縁取り
- `<shadow color=... x=... y=... [add]>` 影
- `<style name='...'>` 名前付きスタイル参照
- `<br>` 改行 / `<sp width=...>` スペース

`add` 属性（値不要のフラグ）を指定すると、既存のレイヤーを置換せず追加する（多段縁取り等）。詳細は `タグ仕様.md` を参照。

エスケープ: `&lt;`, `&gt;`, `&amp;`, `&quot;`, `&apos;`, `&nbsp;`, `&#nnnn;`, `&#xhhhh;`

---

## 5. ビルド成果物

```
richtext_lib (静的ライブラリ)
    └── コア機能全クラス

sample_render.exe (描画サンプル)
    ├── sample/sample_render.cpp
    └── → richtext_lib をリンク

sample_sequential.exe (逐次表示サンプル)
    ├── sample/sample_sequential.cpp
    ├── ParagraphLayout + maxGlyphs による段階的表示
    └── StyledLayout + drawStyledLayout による段階的表示

sample_texture.exe (テクスチャアトラスサンプル)
    ├── sample/sample_texture.cpp
    ├── TextureAtlas + ParagraphLayout によるアトラス描画
    └── TextureAtlas + StyledLayout によるアトラス描画

sample_vertical.exe (縦組みサンプル)
    ├── sample/sample_vertical.cpp
    ├── VerticalLayout による縦1行のベタ組み
    ├── VerticalParagraphLayout による JLReq 組版・禁則・ぶら下げ
    └── InlineAnnotation によるルビ・縦中横・圏点・割注・字取り

sample_vertical_page.exe (縦組みブロック組版サンプル)
    ├── sample/sample_vertical_page.cpp
    └── BlockLayout による段組・連結コンテナ流し込み・ページ分割

richtext_pdf (静的ライブラリ・任意ターゲット)
    ├── CMake オプション RICHTEXT_PDF
    │     単体ビルドでは既定 ON、親プロジェクトに組み込むときは既定 OFF
    └── richtext_lib をリンク（逆は無い）

sample_vertical_pdf.exe (PDF 出力サンプル)
    ├── sample/sample_vertical_pdf.cpp
    └── 1 回だけ組んだ BlockLayout から画面と PDF の両方を出す
```

吉里吉里プラグイン（`richtext.dll`）は `../krkr_richtext` で別プロジェクトとしてビルドする。

---

## 6. 描画順序の設計

Appearance 内の DrawStyle は配列順に描画される。`set*` / `add*` メソッドが管理する正規化された描画順序:

1. オフセット付き Fill（影）を最初に描画（最背面）
2. Stroke（縁取り）を次に描画
3. メインの Fill（本体色）を最後に描画（最前面）

この順序により、影が最も奥に、縁取りがその上に、塗りが最前面に表示される。

`add` 属性付きタグでは既存レイヤーを保持したまま追加することで、多段縁取りや多段影を実現できる。

---

## 6.1 描画バックエンドの差し替え（ベクターエンジンを使いたくなったら）

現在、アウトラインからピクセルまでは自前で完結している。

```
FontBackendFace::getGlyphMask()      アウトライン → 8bit カバレッジマスク
                                     （glyphware = FreeType のラスタライザ / FT_Stroker）
        ↓
RenderTarget::blendMask()            マスク + 色 → ARGB8888（src/Raster.cpp）
```

thorvg のようなベクターグラフィックスエンジンを使いたくなった場合（グラデーション塗り、
クリップパス、ブレンドモード、パスのアニメーション等、マスク合成では扱えない機能が
必要になった場合）、**差し替え口は `GlyphRenderer::blendGlyph()` の 1 箇所**である。

差し替え手順:

1. アウトラインは今も `FontBackendFace::getGlyphOutline()` で取れる。フォントユニット
   （y-up）で `FontOutlineSink` に moveTo / lineTo / quadTo / cubicTo / close が流れる。
   `getGlyphMask()` に渡しているのと同じアフィン行列（`blendGlyph` 内で組み立てている
   `Matrix2D m`）を適用すれば、描画先ピクセル座標のパスになる。
2. `blendGlyph()` の「マスク生成 → `target_.blendMask()`」を、そのパスをベクターエンジンへ
   渡す処理に置き換える。マスクキャッシュは不要になるので `maskCache_` は外してよい。
3. カラー絵文字のベクター経路（COLR）も同様で、`FontFace::renderCOLRv1Glyph()` が
   `getColorLayers()` で得たレイヤーごとに `getGlyphMask()` を呼んでいる部分を、
   アウトライン + 塗り指定としてベクターエンジンへ渡す形にする。
4. `RenderTarget`（矩形塗り・画像ブリット）はそのまま使うか、エンジン側の描画に置き換える。

留意点:

- `getGlyphMask()` に渡す変形は **y-up**（バックエンド＝FreeType の座標系）。`blendGlyph()`
  では y-down の合成行列を組み立ててから y 行を反転して渡し、返ったマスクを
  `(left, -top)` に置いている。ベクターエンジンへ渡す場合は y 行の反転が不要になる。
- COLR のレイヤー行列は **y-down 側の空間**で解釈する（`FontFace::renderCOLRv1Glyph()` の
  コメント参照）。FreeType の COLR 実装に合わせた既存の見た目を維持するための約束事なので、
  エンジンを替えるときもこの解釈を守ること。
- 縁取りは現在 `FT_Stroker`（マスク生成時にストローク済みのアウトラインを作る）。
  ベクターエンジン側のストローカを使う場合、結合・キャップ・マイター限界の既定値
  （`DrawStyle` の `strokeJoin` / `strokeCap` / `miterLimit`）を合わせないと見た目が変わる。

なお thorvg 自体は吉里吉里本体（Elements / layerExVector）が使っており、その thorvg には
ホスト注入のテキストローダ（`TVG_LOADER_GW`）経由で同じ glyphware が繋がっている。
richtext から使いたくなった場合も、フォント実体は本体と共有されたままにできる。


### 6.1.1 thorvg を使う場合の推奨形（アウトライン抽出を二重化しない）

吉里吉里に組み込んだ状態では、**thorvg 側も同じ glyphware を見ている**。thorvg フォークの
gw ローダ（`TVG_LOADER_GW`）はホスト注入のブリッジ `TvgGwBridge`
（`inc/thorvg_gw_bridge.h`）からフォントを引いており、ブリッジには既に

```c
void*    (*openFaceByKey)(void* ctx, const char* keyU8);          // ホストキーで face を開く
uint32_t (*glyphIndex)(void* ctx, void* face, uint32_t codepoint);
int      (*glyphOutline)(void* ctx, void* face, uint32_t gid, float scale,
                         const TvgGwPathSink* sink);              // face + gid でアウトライン
```

が用意されている。ブリッジ本体は本体が `TVPGetFontTvgBridge()` で公開しており、
自前 thorvg を静的リンクするプラグイン（layerExVector 等）は
`tvgGwSetBridge(TVPGetFontTvgBridge())` で自分のコピーへ注入している。

つまり **richtext がアウトラインを取り出して thorvg のパスに詰め替える必要は無い**。
グリフは「face ハンドル + グリフID」で指せば、thorvg 側が同じ FT_Face から
アウトラインを生成できる。この形なら:

- アウトライン抽出が richtext と thorvg で二重にならない
- パスデータ（数百点/グリフ）をプラグイン境界越しに毎回コピーしない
- FT_Face・フォントバイト列は本体と共有されたまま

不足しているのは thorvg 側の**公開入口**だけ。現状の公開 API は
`Shape::appendPath(cmds, pts)`（パスを渡す）と `Text`（文字列を渡すと thorvg 自身が
シェイピング・行分割する）しかなく、richtext のように**レイアウト済みのグリフを
1 つずつ描く**入口が無い。フォークに次のようなものを足すのが筋:

```cpp
// 例: gw ブリッジ経由でグリフのアウトラインを Shape に積む
Result Shape::appendGlyph(void* face, uint32_t glyphId, float scale) noexcept;
```

（`Text` はレイアウトまで抱えてしまうので使えない。minikin が算出した配置を尊重する必要がある）

richtext 側で必要になる追加:

- `FontBackendFace` に**ホストのフォントキー**を返す口（`openFaceByKey` に渡すため）。
  現状 `GlyphwareFace` / `HostFace` はキーを内部に持っているだけで公開していない
- `GlyphRenderer::blendGlyph()` を「マスク合成」から「Shape 生成 + appendGlyph + 変形 + 塗り」に
  差し替える経路（6.1 の手順どおり、差し替え口は 1 箇所）

なお thorvg 非依存のまま置いておきたい構成（単体ビルド・非吉里吉里ホスト）も残るので、
**マスク合成経路と thorvg 経路は択一のビルド切り替え**にするのが扱いやすい。

---

## 7. ルビ（振り仮名）の設計

### 7.1 概要

ルビはベーステキストの上部に小さな文字で読み仮名等を表示する機能。minikin にはルビのネイティブサポートがないため、描画層（`TextRenderer`）で独自に実装する。

### 7.2 タグ書式

```
<ruby text='ふりがな'>漢字</ruby>
```

- `text` 属性: ルビとして表示する文字列（UTF-8/UTF-16）
- タグ内のテキスト: ルビが付くベーステキスト

### 7.3 パース

`TagParser` が `<ruby>` タグを解析し、`TextSpan` に以下を設定:

| フィールド | 型 | 内容 |
|-----------|-----|------|
| `hasRuby` | `bool` | `true` |
| `rubyText` | `std::u16string` | ルビ文字列 |

### 7.4 レイアウト・描画

`TextRenderer::drawStyledText` 内で、ルビ付きセグメントの描画後にルビを追加描画する。

**ルビレイアウト処理:**

1. ルビ用 `TextStyle` を生成（ベーススタイルの `fontSize × rubyScale`）
2. ルビ文字列を `TextLayout` で独立にレイアウト
3. ルビを水平方向にベーステキスト上で中央揃え配置
4. ルビを垂直方向にベーステキストの ascent 直上に配置

**配置計算:**

```
rubyX = segmentX + (segmentWidth - rubyWidth) / 2
rubyY = baseY + baseAscent + rubyDescent
```

- `baseAscent` は負値（上方向）なので、ベースラインから上に移動
- `rubyDescent` は正値なので、ルビの下端がベーステキストの上端に接する

### 7.5 パラメータ

`TagParser::ParseOptions` で調整可能:

| パラメータ | デフォルト | 説明 |
|-----------|-----------|------|
| `rubyScale` | `0.5` | ルビのフォントサイズ倍率（ベースサイズに対する比率） |

### 7.6 行間との関係

ルビは行の ascent・descent や行間計算に影響を与えない。ルビが正しく表示されるためには、呼び出し側で十分な行間を確保する必要がある。

**1行目の上方はみ出し:** パラグラフ領域の1行目ではルビがベーステキストの ascent より上に描画されるため、領域の上端からはみ出す。はみ出しを避けるには、描画領域の y 座標をルビ分（`fontSize × rubyScale` 程度）下げて調整する。

### 7.7 ルビの重なり回避

隣接する文字に別々の `<ruby>` タグを付けると、ルビテキストがベーステキストより長い場合にルビ同士が重なる。

```
<!-- NG: ルビが重なる可能性あり -->
<ruby text='とうきょう'>東京</ruby><ruby text='タワー'>塔</ruby>

<!-- OK: ベーステキストをまとめて1つの ruby タグで囲む -->
<ruby text='とうきょうタワー'>東京塔</ruby>
```

ルビ幅がベーステキスト幅を超える場合のレイアウト調整（字間拡張や圧縮）は行わないため、ルビが長くなる場合はベーステキストを広くとるか、1つの `<ruby>` タグにまとめて対処する。

### 7.8 制限事項

- **行またぎ非対応**: ルビ付きテキストが行分割で複数行にまたがった場合、ルビは描画されない（セグメントがスパン全体をカバーする場合のみ描画）
- **ルビ幅の行分割への影響なし**: ルビがベーステキストより長い場合でも、行分割計算にルビ幅は考慮されない（はみ出す可能性がある）
- **グループルビのみ**: 1文字ずつルビを対応させるモノルビには非対応（グループ全体に対して一括配置）

---

## 8. 双方向テキスト（Bidi）の設計

### 8.1 概要

Unicode Bidirectional Algorithm（UBA）に基づき、LTR（左から右）と RTL（右から左）のテキストを混在して描画する。minikin 内部の ICU `ubidi` を使用してテキストのラン分割と視覚順序の決定を行う。

### 8.2 Bidi モード

`TextStyle::bidi` で段落方向を指定する。minikin の `Bidi` 列挙値に対応:

| 値 | ICU レベル | 動作 |
|---|---|---|
| `Bidi::LTR` | 0 | 段落方向を LTR に強制 |
| `Bidi::RTL` | 1 | 段落方向を RTL に強制 |
| `Bidi::DEFAULT_LTR` | `UBIDI_DEFAULT_LTR` | 最初の強い文字で段落方向を決定。強い文字がなければ LTR |
| `Bidi::DEFAULT_RTL` | `UBIDI_DEFAULT_RTL` | 最初の強い文字で段落方向を決定。強い文字がなければ RTL |

### 8.3 DEFAULT_LTR / DEFAULT_RTL の注意

`DEFAULT_LTR` と `DEFAULT_RTL` はいずれも「最初の強い方向性文字」で段落方向を決定する。違いは強い文字が**存在しない**場合のフォールバックのみ。

例: テキスト `"Hello مرحبا World"` の場合、最初の強い文字は `H`（LTR）であるため、`DEFAULT_LTR` でも `DEFAULT_RTL` でも段落方向は LTR になる。明示的に RTL 段落にするには `Bidi::RTL` を使用する。

### 8.4 CJK 文字の Bidi クラス

CJK 文字（漢字・ひらがな・カタカナ）の Unicode bidi クラスは **"L"（Left-to-Right）** である。これは Latin 文字と同じ扱いとなる。

RTL 段落内での動作:
- **ラン（ブロック）の配置順** は段落方向に従って入れ替わる（例: `[日本語][العربية][日本語]` → RTL では `[日本語][العربية][日本語]` の並び順が逆転）
- **CJK ラン内部の文字順** は常に LTR のまま維持される（「日本語」が「語本日」にはならない）

これは Unicode 標準に基づく正しい動作である。日本語の右から左表記は伝統的な縦書きや看板等に限られ、Unicode の水平テキスト処理では LTR として扱われる。

### 8.5 制限事項

- **`FORCE_LTR` / `FORCE_RTL` は使用不可**: minikin 内部でアサーション違反となるため、`Bidi::LTR` / `Bidi::RTL` を使用すること
- **段落分割との関係**: `\n` で分割された各サブ段落は独立した bidi コンテキストを持つ

---

## 9. 絵文字フォントの設計

### 9.1 概要

絵文字フォントには大きく分けて3種類の形式がある。本ライブラリは CBDT と COLRv1 のカラー絵文字、およびモノクロアウトライン絵文字に対応する。

| 形式 | 例 | 特徴 | 対応方法 |
|------|-----|------|---------|
| **CBDT/CBLC** | NotoColorEmoji.ttf | 固定サイズビットマップ（PNG）を内包 | FreeType の `FT_LOAD_COLOR` で取得 |
| **COLRv1** | Noto-COLRv1.ttf | ベクターベースのペイントグラフ（DAG） | バックエンドがレイヤー列に展開し、マスク合成でレンダリング |
| **モノクロ** | NotoEmoji-Regular.ttf | 通常のアウトラインフォント | 通常のグリフパス描画 |

### 9.2 カラー絵文字の判定

`FontFace::isColorGlyph()` で判定する。FreeType の `FT_Load_Glyph` に `FT_LOAD_COLOR` フラグを渡し、結果のビットマップが `FT_PIXEL_MODE_BGRA` であるか、または `FT_Get_Color_Glyph_Paint` が成功する（COLRv1 テーブルを持つ）場合にカラーグリフとみなす。

### 9.3 CBDT ビットマップ絵文字

NotoColorEmoji.ttf は 128×128 ピクセルの固定サイズ PNG ビットマップを内包する。

**取得手順:**

1. `FT_Select_Size(face, 0)` で固定サイズを選択
2. `FT_Load_Glyph` に `FT_LOAD_COLOR` を指定（`FT_LOAD_RENDER` は不要）
3. `FT_Bitmap` から BGRA データを RGBA に変換

**スケーリング:**

ビットマップは固定サイズ（`available_sizes[0].height` = 128px）で取得される。要求フォントサイズに合わせるスケール係数を `GlyphBitmap::strikeHeight` に格納し、描画時に `fontSize / strikeHeight` でスケーリングする。

**ベースライン位置:**

FreeType の `bitmap_left`（bearingX）と `bitmap_top`（bearingY）をスケール係数で乗算して描画位置を決定する。

### 9.4 COLRv1 ベクター絵文字

Noto-COLRv1.ttf は COLRv1 テーブルにベクターベースのペイントグラフを格納する。FreeType 2.13+ はペイントグラフの走査 API を提供するが、自動レンダリングモジュールは含まない。そのため `FontFace::renderCOLRv1Glyph()` で自前レンダリングを行う。

**レンダリング手順:**

1. `FontBackendFace::getColorLayers()` を呼ぶ（内部では glyphware がペイントグラフを再帰走査し、「アウトライングリフ + 変換 + 塗り」の背面→前面レイヤー列に平坦化する。クリップボックスも同時に返る）
2. クリップボックスから一時 ARGB バッファ（`RenderTarget`）を確保
3. 各レイヤーについて `getGlyphMask()` でカバレッジマスクを生成（レイヤー行列を変形として渡す）
4. 塗り（Solid / LinearGradient / RadialGradient）に応じてマスクを色付き合成
5. ARGB バッファを RGBA に変換して `GlyphBitmap` に格納

**座標系の整合:**

- グリフアウトライン: `FT_LOAD_NO_SCALE` でデザイン単位（フォント単位）のまま取得
- ルートトランスフォーム: `FT_COLOR_INCLUDE_ROOT_TRANSFORM` に含まれる行列がデザイン単位→ピクセル単位への変換を担当
- 内部トランスフォーム（Translate / Scale / Rotate / Skew）: デザイン単位空間で適用され、親行列と合成

**対応ペイントタイプ:**

| ペイントタイプ | 説明 |
|--------------|------|
| `COLR_LAYERS` | 複数レイヤーの SRC_OVER 合成 |
| `GLYPH` | グリフアウトラインを形状として使用 |
| `SOLID` | 単色塗り（CPAL パレット参照） |
| `LINEAR_GRADIENT` | 線形グラデーション |
| `RADIAL_GRADIENT` | 放射グラデーション |
| `COLR_GLYPH` | 別のカラーグリフを参照 |
| `TRANSFORM` | アフィン変換 |
| `TRANSLATE` | 平行移動 |
| `SCALE` | 中心点指定のスケーリング |
| `ROTATE` | 中心点指定の回転 |
| `SKEW` | シアー変換 |
| `COMPOSITE` | 合成（backdrop → source 順で描画） |

### 9.5 フォント登録の推奨構成

```cpp
// カラー絵文字（CBDT ビットマップ）
fontManager.registerFont("NotoColorEmoji.ttf", "emoji");

// カラー絵文字（COLRv1 ベクター）
fontManager.registerFont("Noto-COLRv1.ttf", "emoji-colr");

// モノクロ絵文字
fontManager.registerFont("NotoEmoji-Regular.ttf", "emoji-mono");
```

フォントコレクションでは絵文字フォントをフォールバック末尾に配置する:

```cpp
auto collection = fontManager.createCollection({"sans", "ja", "emoji"});
```

### 9.6 制限事項

- **COLRv1 の COMPOSITE**: 現在は backdrop → source を単純に重ね描きしており、Porter-Duff 合成モードには未対応
- **COLRv1 のグラデーション座標**: グラデーション座標はデザイン単位のまま使用しているため、一部の複雑なグラデーション絵文字で描画が不正確になる場合がある
- **CBDT のサイズ**: 固定サイズ（128px）からのスケーリングのため、大サイズでの表示時にジャギーが発生する
- **COLRv0**: COLRv0（単純レイヤー方式）には非対応。COLRv0 のみのフォントではフォールバックされない

---

## 10. バリアブルフォントの設計

### 10.1 概要

バリアブルフォント（OpenType Variable Font）は1つのフォントファイルに複数の軸（weight, width, italic 等）を持ち、連続的なスタイル変化が可能なフォント形式。本ライブラリは以下の3軸に対応する。

| 軸タグ | 名称 | TextStyle フィールド | 範囲（一般的） | 備考 |
|--------|------|---------------------|----------------|------|
| `wght` | Weight | `fontWeight` | 100–900 | 太さ |
| `ital` | Italic | `italic` | 0 or 1 | イタリック |
| `wdth` | Width | `fontWidth` | 50–200 (%) | 字幅 |

各軸について、バリアブルフォントの軸が利用可能な場合はそれを使い、利用不可能な場合はフェイク処理（ストローク太字・シアー変換・水平スケール）で代替する。

### 10.2 アーキテクチャ

**フォント登録:**

1. `FontFace` コンストラクタで `FT_Get_MM_Var` によりバリアブルフォントの軸情報を取得し、`axes_` に格納。`wdth` 軸がある場合は `hasWdthAxis_`, `wdthMin_`, `wdthMax_`, `wdthDefault_` も保存
2. `FontFace::setVariations()` で `FT_Set_Var_Design_Coordinates` を呼び出し、FreeType のフォントフェイスに軸値を設定
3. `FontManager::registerVariableFont()` で同じフォントファイルから異なるウェイト・スタイルの FontFace を複数作成。`wght` 軸と `ital` 軸を設定
4. `FontManager::createCollection()` が同じ名前の全 FontEntry を1つの `minikin::FontFamily` にまとめ、`Font::Builder::setWeight().setSlant()` でウェイトとスラントを明示指定

**フォント選択（minikin 自動）:**

5. minikin が `TextStyle::fontWeight` と `TextStyle::italic` に基づいて最適な FontFace を自動選択
6. 要求に合致する FontFace がない場合、minikin が `FontFakery` フラグ（`isFakeBold()`, `isFakeItalic()`）を設定

**使用例:**

```cpp
// ウェイト別・スタイル別に登録（Upright 6ウェイト + Italic 6ウェイト）
for (uint16_t w : {100, 300, 400, 500, 700, 900}) {
    fm.registerVariableFont("NotoSans-VF.ttf", "sans-vf", w);           // Upright
    fm.registerVariableFont("NotoSans-Italic-VF.ttf", "sans-vf", w, true); // Italic
}
auto col = fm.createCollection({"sans-vf", "emoji"});

style.fontWeight = 700;        // → wght=700 の FontFace が選択される
style.italic = true;           // → ital=1 の FontFace が選択される
style.fontWidth = 75.0f;       // → wdth 軸 75% で描画（condensed）
```

### 10.3 wght 軸（ウェイト）とフェイクボールド

**バリアブルフォント wght 軸:**

`registerVariableFont()` でウェイトごとに FontFace を作成し、minikin のフォント選択に任せる。minikin は `fontWeight` に最も近いウェイトの FontFace を選択する。

**フェイクボールド（フォールバック）:**

バリアブルフォントや Bold ウェイトのフォントが登録されていない場合、minikin が `FontFakery::isFakeBold()` フラグを自動設定する（要求ウェイト ≥ 600 かつ実際のウェイトとの差 ≥ 200）。

`GlyphRenderer` はこのフラグを検出すると、グリフパスに同色ストローク（フォントサイズの 1/24 幅）を重ねて太字をシミュレートする。

```
品質の優先度（高→低）:
1. バリアブルフォント wght 軸 — フォント設計者が意図した太さ
2. ウェイト別フォントファイル — Bold 用に設計された別ファイル
3. フェイクボールド — ストローク太らせ（セリフの潰れや字間変化あり）
```

### 10.4 ital 軸（イタリック）とフェイクイタリック

**バリアブルフォント ital 軸:**

`registerVariableFont()` の `italic=true` 引数で、Italic 用 FontFace（`ital` 軸=1、`FontStyle::Slant::ITALIC`）を登録する。minikin が `TextStyle::italic=true` のとき自動的に Italic FontFace を選択し、実際のイタリック字形が使用される。

イタリック軸を持つ別ファイル（例: `NotoSans-Italic[wdth,wght].ttf`）を同じ名前で登録することで、Upright と Italic の両方のフォントが1つの FontFamily にまとまる。

**フェイクイタリック（フォールバック）:**

Italic FontFace が登録されていない場合、minikin が `FontFakery::isFakeItalic()` フラグを設定する。

`GlyphRenderer` はこのフラグを検出すると、グリフパスに X 方向のシアー変換（`skewX = -0.25`、約14度の右傾斜）を適用してイタリックをシミュレートする。

```
品質の優先度（高→低）:
1. バリアブルフォント ital 軸 — フォント設計者が意図したイタリック字形
2. イタリック専用フォントファイル — 別ファイルで提供
3. フェイクイタリック — シアー変換（字形は直立のまま傾けるだけ）
```

> **注意**: CJK フォント（NotoSansJP 等）は一般にイタリック軸を持たないため、`italic=true` を指定するとフェイクイタリックが適用される。

### 10.5 wdth 軸（字幅）とフェイク幅

**TextStyle::fontWidth:**

`TextStyle::fontWidth` でフォント幅をパーセント指定する（100.0 = 通常幅）。CSS の `font-stretch` に相当する。

| 値 | CSS 相当 |
|----|----------|
| 50 | ultra-condensed |
| 75 | condensed |
| 100 | normal |
| 125 | expanded |
| 150 | extra-expanded |

**描画時の処理（GlyphRenderer）:**

`fontWidth` が 100 以外の場合、以下のロジックで wdth 軸とフェイクスケールを組み合わせる:

```
requestedWidth = style.fontWidth

if (フォントに wdth 軸あり、範囲 [min, max]):
    clampedWidth = clamp(requestedWidth, min, max)
    font->applyWidth(clampedWidth)           // wdth 軸を設定
    fakeScaleX = requestedWidth / clampedWidth  // 軸で足りない分
else:
    fakeScaleX = requestedWidth / 100.0      // 全てフェイク
```

例: NotoSans VF（wdth 軸 62.5–100）の場合:

| fontWidth | wdth 軸値 | fakeScaleX | 説明 |
|-----------|-----------|------------|------|
| 40% | 62.5 | 0.64 | 軸限界 + フェイク縮小 |
| 80% | 80 | 1.0 | 軸のみで対応 |
| 100% | 100 | 1.0 | 通常（変更なし） |
| 150% | 100 | 1.5 | 軸限界 + フェイク拡大 |

**レイアウト時の処理（MinikinPaint + GetHorizontalAdvance）:**

`TextStyle::applyTo` で `paint.fontWidth` を設定する。`fontWidth` は minikin の `MinikinPaint` に追加したフィールドで、`FontFace::GetHorizontalAdvance` 内で参照される。

`GetHorizontalAdvance` は fontWidth に基づき wdth 軸を設定してからグリフをロードし、軸で対応できない残りの fake 分を advance に乗算して返す。これにより行分割（MeasuredText）とグリフ配置（Layout）の両方で fontWidth が正確に反映される。

**キャッシュ:**

minikin の `LayoutCacheKey` と `GlyphRenderer` のグリフキャッシュキーの両方に `fontWidth` を含めることで、異なる幅のレイアウト・グリフが正しくキャッシュされる。

```
品質の優先度（高→低）:
1. バリアブルフォント wdth 軸 — フォント設計者が意図した字幅変化
2. フェイク幅（水平スケール） — 字形を均一に伸縮（ストロークの太さも変化する）
```

### 10.6 FontFace の wdth 軸 API

```cpp
// wdth 軸の有無と範囲を取得
bool getWidthAxisRange(float& minWidth, float& maxWidth) const;

// wdth 軸の値を設定（他の軸は維持）
void applyWidth(float width) const;
```

`applyWidth()` は `const` メソッドだが、内部で FT_Face の可変状態を変更する。シングルスレッド前提の設計。

---

## 11. 縦組みの設計

設計の意図・判断の根拠・フェーズ計画は `縦組み設計.md` に、実装の進捗と積み残しは
`実装.md` の「縦組みフェーズ」にある。ここは**クラスの一覧と入口**だけを置く。

横組み経路（`TextLayout` / `ParagraphLayout` / `StyledLayout`）には手を入れていない。
縦組みはその隣に並ぶ別系統で、ヘッダは `include/richtext/vertical/`、
名前空間は `richtext::vertical`。

### 11.1 座標系

縦組みの組版結果は「縦ベースライン」を基準にした 2 軸で表す。

```
  u 軸 … 行に直交する方向。0 = 縦ベースライン（列の中心線）、右が正
  v 軸 … 行が進む方向。0 = 行頭（上端）、下が正
```

`GlyphInfo` へは u を `x`、v を `y` に入れて渡す。描画時に列の中心 X と行頭 Y を
足せばそのままピクセル座標になる（描画先は y-down）。列そのものの送り
（vertical-rl なら右から左へ）は `VerticalParagraphLayout` / `BlockLayout` の担当。

### 11.2 クラス一覧

| ヘッダ | 役割 |
|---|---|
| `WritingMode.hpp` | `WritingMode` / `TextOrientation` / `CharOrientation`、座標系の定義、UAX #50 相当の正立／横倒し判定 |
| `VerticalShaper.hpp` | 縦組みシェイピング。`FontCollection::itemize()` でフォールバックを解決し、HarfBuzz を直接叩く。出力は `GlyphInfo` 列と `ShapedCluster` 列 |
| `VerticalLayout.hpp` | 縦 1 行のベタ組み。横組みの `TextLayout` に対応 |
| `CharClass.hpp` | JLReq 附属書 A の文字クラス、仮想ボディ内の字面の寄り、行頭禁則／行末禁則 |
| `SpacingTable.hpp` | JLReq 表 3 のアキ量を「自然値・伸び・縮み」（`GlueSpec`、em 単位）で返す |
| `LineItem.hpp` | Box / Glue / Penalty。合法ブレーク点の規則は TeX と同じ |
| `InlineAnnotation.hpp` | ルビ・縦中横・圏点・割注・字取りの指定（本文の文字範囲に対する注記） |
| `LineItemBuilder.hpp` | クラスタ列＋注記 → 組版アイテム列。約物の詰め・禁則・ぶら下げ・インライン要素がここで確定する |
| `LineBreaker.hpp` | 自前の行分割（Greedy / Knuth–Plass）。グルーの調整比を返す |
| `VerticalParagraphLayout.hpp` | 段落単位。行分割から配置まで通し、行ごとの `GlyphInfo` 列を作る |
| `BlockLayout.hpp` | 段組・連結コンテナへの流し込み・ページ分割 |

### 11.3 入口

用途に応じて 3 段階ある。下ほど上位で、上位は下位を内部で使う。

```cpp
// 1 行だけ（ベタ組み。約物の詰めは無い）
vertical::VerticalLayout layout;
layout.layout(text, style, vertical::TextOrientation::Mixed);
renderer.drawVerticalLayout(layout, centerX, topY, appearance);

// 段落（JLReq 組版・行分割・両端揃え・インライン注記）
vertical::VerticalParagraphLayout para;
para.layout(text, annotations, style, lineLength, options);
renderer.drawVerticalParagraphLayout(para, originX, originY, appearance);

// 版面へ流し込む（段組・ページ分割）
vertical::BlockLayout block;
block.setContainers(containers);
block.layout(paragraphs);
renderer.drawBlockContainer(block, pageIndex, offsetX, offsetY, appearance);
```

`VerticalLayoutOptions` が組版の指定をまとめて持つ。

```cpp
struct VerticalLayoutOptions {
    WritingMode writingMode;          // VerticalRl（既定）/ VerticalLr
    TextOrientation orientation;      // Mixed（既定）/ Upright / Sideways
    VerticalSpacingOptions spacing;   // punctuationSpacing / hangingPunctuation
                                      // / latinGap / letterSpacing
    LineBreakOptions lineBreak;       // strategy（Greedy 既定 / KnuthPlass）/ justify
    float lineGap;                    // 行間。行送り = fontSize + lineGap
};
```

### 11.4 GlyphInfo の拡張

縦組みのために `GlyphInfo` へ 4 つ足した。横組み経路では既定値のままなので影響しない。

| フィールド | 意味 |
|---|---|
| `rotation` | 回転角（ラジアン）。数学慣習（y-up）の反時計回りが正。欧文の横倒しは -90° |
| `scaleX` / `scaleY` | グリフ単位のスケール。縦中横の圧縮に使う |
| `fontSize` | このグリフ固有のサイズ。0 なら描画時のスタイルのサイズを使う。1 行の中に本文・ルビ・圏点・割注の 4 サイズが混ざるので必要になった |

変形の組み立ては `include/richtext/GlyphTransform.hpp` に切り出してあり、
`GlyphRenderer` と `pdf::PdfWriter` が同じものを使う。ここが 2 箇所に分かれていると
画面と PDF で斜体や横倒しがずれる。

---

## 12. PDF 出力の設計

`richtext_pdf`（CMake オプション `RICHTEXT_PDF`）。単体ビルドでは既定 ON、親プロジェクトに
組み込むときは既定 OFF。`richtext_lib` に依存するが逆は無いので、吉里吉里プラグインは
これをリンクしない。

組版層の出力（`GlyphInfo` の列）を、ラスタライズ backend と並んで受け取る。
**同じ組版結果から画面と PDF が出る**ようにするのが目的で、既存 PDF ライブラリに
文字列を渡す方式は採らない（ライブラリ側が再シェイピングして組版結果が崩れる）。

### 12.1 PdfWriter

`TextRenderer` と同じ引数・同じ座標系（左上原点・y-down、1px = 1pt）で呼べる。

```cpp
pdf::PdfWriter writer;
writer.beginPage(width, height);
writer.drawGlyphs(glyphs, x, y, style, appearance, &sourceText);
writer.drawVerticalParagraph(para, originX, originY, appearance);
writer.drawBlockContainer(block, pageIndex, 0, 0, appearance);
writer.endPage();
writer.save("out.pdf");
```

`sourceText` は省略できるが、渡すと ToUnicode CMap が埋まって PDF 上で文字の
選択・検索ができるようになる。

### 12.2 フォント埋め込み

`Identity-H` を使うので、2 バイトのコード＝CID＝グリフ ID になる。組版層が持っている
グリフ ID をそのまま書ける。

| アウトライン | 記述子 | フォントファイル |
|---|---|---|
| CFF（.otf） | `CIDFontType0` | `/FontFile3` `/Subtype /OpenType` |
| glyf（.ttf） | `CIDFontType2`＋`/CIDToGIDMap /Identity` | `/FontFile2` |

CFF か glyf かの判定と `FontDescriptor` の値（FontBBox・Ascent・Descent・CapHeight・
ItalicAngle・PANOSE）は `src/pdf/SfntInfo.cpp` が sfnt のテーブルディレクトリを
直接読んで取る。サブセット化はしない（full embed）。

### 12.3 配置

1 グリフずつ `Tm` で置く。縦組みでも `Identity-V` は使わない — 縦メトリクスは
組版層で計算済みなので、PDF 側に再計算させない。

画面（y-down）の変形行列を PDF（y-up）へ移すときは Y 反転で共役を取る。
シアー成分の符号が入れ替わり、横倒しの回転もそのまま乗る。

```
a_pdf =  glyphMat.e11     c_pdf = -glyphMat.e12
b_pdf = -glyphMat.e21     d_pdf =  glyphMat.e22
```

**`Tm` にフォントサイズを入れてはいけない。** `Tf` が掛けるので二重に効く
（18pt が 324pt で出る）。`Tm` の 2x2 はグリフ固有の変形だけを入れる。

装飾は PDF のテキスト描画モードへ写す。塗りは `0 Tr`、縁取りは `1 Tr`＋線幅、
フェイクボールドは `2 Tr`（塗り＋同色の縁取り）。透明度は ExtGState の `/ca` `/CA`。

### 12.4 制限事項

- サブセット化なし。和文フォント 1 つで 20MB を超える
- コンテンツストリームは無圧縮。`TJ` でまとめず 1 グリフ 1 `Tm` なので冗長
- バリアブルフォントの wdth 軸は PDF へ持ち出せない（インスタンス化が必要）。
  フォント幅はすべて水平スケールとして書き出す
- TTC は 1 フォントを切り出さないと埋め込めないので未対応（警告を出して飛ばす）
- CID-keyed CFF で charset が identity でないフォントは CID→GID がずれうる
  （検証した Noto CJK は identity）
- 画像・リンク・しおり・ページラベルは未対応
