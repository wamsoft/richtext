# 縦組み設計

richtext に日本語縦組み（縦書き）組版を追加するための設計資料。
既存の横組み経路（`TextLayout` / `ParagraphLayout` / `StyledLayout`）は変更せず、
その隣に縦組み経路を追加する。

到達目標は **JLReq（W3C 日本語組版処理の要件）水準の縦組み**であり、
画面描画と PDF 出力を **同一の組版結果**から出すことを設計の中心に置く。

---

## 1. 方針

### 1.1 「1文字＝1em の箱」をやらない

日本語組版でよくある実装は、1 文字を 1em 角の箱に入れて並べる方式である。
実装は簡単だが、この方式では原理的に次が実現できない。

- 約物の詰め（`「」。、` の半角取り、連続約物のアキ調整）
- 行頭括弧の半角下げ、行末句読点のぶら下げ
- 和欧間の四分アキ
- **行長調整をアキの伸縮で行う（追い込み／追い出し）**
- `palt` / `vpal` によるプロポーショナル和文、`vkrn` による縦カーニング
- ルビの前後文字への掛かり（はみ出し）

そこで本設計では、和文組版を **グリフ配置の問題ではなく
「文字クラス間のアキ（グルー）とペナルティの列」を解く問題**として扱う。
これは pTeX の JFM（和文フォントメトリック）および TeX の
Box / Glue / Penalty モデルと同じ考え方で、JLReq の実装形として最も枯れている。

### 1.2 組版結果は「配置済みグリフ列」で切る

組版層の出力を `GlyphInfo`（グリフ ID・フォント・座標・変換）の列に統一し、
その下にラスタライズ backend と PDF backend を並列に置く。
画面と PDF で組版が二重実装にならないようにするための最重要の分割線。

```
                  縦組み組版層（本設計）
                          │
              配置済みグリフ列 (GlyphInfo[])
                    ┌─────┴─────┐
       glyphware/FreeType      PDF writer
        → Raster / Atlas        → Type0 / Identity-H
```

---

## 2. 現状コードの制約（調査で確定した事実）

| 事実 | 出典 | 影響 |
|---|---|---|
| minikin のシェイピングは LTR / RTL のみ。TTB を扱わない | `ext/minikin/libs/minikin/LayoutCore.cpp:456` | 縦組みのシェイピングは自前で行う必要がある |
| `FontCollection::itemize()` は public | `ext/minikin/include/minikin/FontCollection.h:45` | **スクリプト分割とフォントフォールバックは minikin を再利用できる** |
| HarfBuzz は `ext/minikin/ext/harfbuzz` にあり、`minikin/HbUtils.h` が `hb_font_t` の RAII を提供 | | 自前で TTB シェイピングを書ける |
| `MinikinPaint::fontFeatureSettings` は存在するが、非空だと `skipCache()` が true になる | `MinikinPaint.h:63,75` | feature 経由で `vert` を掛ける方式はレイアウトキャッシュを殺す。採らない |
| `FontGlyphMetrics::advanceY` が既にある | `include/richtext/FontBackend.hpp:67` | 縦メトリクスの供給口は用意済み |
| `GlyphRenderer` は変形を 1 つのアフィン行列に畳み込み、マスクキャッシュのキーは「2x2 成分 + 1/4px 位相」 | `CLAUDE.md` / `設計.md 6` | **グリフ回転はキャッシュ設計を変えずに載る** |
| `ParagraphLayout` は minikin の `LineBreaker`（Greedy / Knuth-Plass）を使用 | `src/ParagraphLayout.cpp` | 縦組みでは使わない（後述 4.3） |

---

## 3. アーキテクチャ

既存レイヤーへの追加位置：

```
タグ解析層        TagParser                （共通・縦横で変更なし）
────────────────────────────────────────────────────────
組版層  横: StyledLayout / ParagraphLayout / TextLayout   （既存）
        縦: BlockLayout / VerticalLineBreaker / VerticalLayout  ★新規
────────────────────────────────────────────────────────
描画層  GlyphRenderer / Raster / TextureAtlas   （共通・回転対応を追加）
        PdfWriter                                ★新規（任意ターゲット）
────────────────────────────────────────────────────────
フォント層  FontFace / FontManager / FontBackend （共通・縦メトリクス取得を追加）
```

新規ファイル構成（予定）：

```
include/richtext/vertical/
  WritingMode.hpp     書字方向と座標系
  CharClass.hpp       JLReq 文字クラス分類
  SpacingTable.hpp    文字クラス間アキ量表
  LineItem.hpp        Box / Glue / Penalty
  VerticalShaper.hpp  縦字形置換と縦メトリクス
  LineBreaker.hpp     自前行分割（追い込み／追い出し）
  VerticalLayout.hpp  1 行の縦配置
  BlockLayout.hpp     段組・流し込み・ページ分割
src/vertical/*.cpp
```

---

## 4. 中核モデル

### 4.1 JLReq 文字クラス

各文字を JLReq の文字クラス（開き括弧・閉じ括弧・句点・読点・中点・分離禁止・
行頭禁則・行末禁則・和字・欧文・数字 …）に分類する。
分類表は Unicode コードポイント範囲＋例外テーブルで持ち、データとして外に出す。

### 4.2 アキ量表 → Box / Glue / Penalty 列

文字クラスの隣接ペアからアキ量（自然値・伸び・縮み）を引き、
文字列を次の 3 種のアイテム列に変換する。

| アイテム | 内容 |
|---|---|
| `Box` | 固定幅。グリフ（クラスタ）1 個 |
| `Glue` | 自然値・stretch・shrink を持つ可変アキ |
| `Penalty` | 分割の可否とコスト。`+INF` = 分割禁止、`-INF` = 強制分割 |

代表例（`em` はフォントサイズ基準）：

- 句読点 `、。` → `Box(0.5em) + Glue(0.5em, stretch 0, shrink 0.5em)`
  （全角ベタ組みが自然値、詰めると半角まで縮む）
- 開き括弧 `「(` → `Glue(0.5em, shrink 0.5em) + Box(0.5em)`
- 行頭の開き括弧 → 先頭 Glue を落として半角下げ
- 和欧間 → `Glue(0.25em, stretch 0.125em, shrink 0.25em)`
- 分離禁止（`……` `——` 連数字）→ 間に `Penalty(+INF)`

この表現にした時点で、**追い込み・追い出し・両端揃えは
「グルーの伸縮でどう行長に合わせるか」という 1 つの問題に統一される**。

### 4.3 行分割

minikin の `LineBreaker` は**使わない**。理由は伸縮グルーを扱えず、
文字アドバンスの単純累積で行長を決めるため、追い込み／追い出しが表現できないこと。

自前で TeX 型の行分割を書く。

1. **Greedy**（既定・高速）— 画面のリアルタイム描画向け
2. **Total-fit（Knuth–Plass）** — PDF 台本向け。段全体でバッドネスを最小化

分割候補の生成：

- 和文は文字クラスから直接求まる（ICU 不要）
- **欧文単語の内部を割らないためだけに** ICU の単語境界を使う
  （minikin の `WordBreaker` 経由、または ICU 直）

ぶら下げ（句読点を版面外に出す）は、句読点直後に
「`Penalty(-INF)` かつ句読点の幅を行長計算から除外する」ブレークとして表現する。

### 4.4 縦組みシェイピング

**minikin の `FontCollection::itemize()` でランに分割してフォールバックを解決し、
各ランを HarfBuzz に `HB_DIRECTION_TTB` で直接シェイピングさせる。**
`fontFeatureSettings` に `vert` を渡す方式は採らない（レイアウトキャッシュが無効化されるため、
および `vkrn` / `vpal` が縦方向 GPOS として効かないため）。

TTB でシェイピングすることで以下が HarfBuzz 側で自動的に効く。

- `vert` / `vrt2` による縦組み字形置換（括弧・長音・句読点）
- `vmtx` / `VORG` による縦アドバンスと原点補正
- `vkrn`（縦カーニング）、`vpal` / `vhal`（プロポーショナル縦メトリクス）

フォントに縦組みテーブルが無い場合は、`FontFaceMetrics` から縦メトリクスを合成する
（アドバンス = 1em、原点 = 水平原点から `ascender` 分だけ上へ）。

### 4.5 グリフの回転とスケール

`GlyphInfo` に変換情報を追加する。

```cpp
struct GlyphInfo {
    // ... 既存フィールド ...
    float rotation = 0.0f;   // ラジアン。縦組み中の欧文横倒し = -90°
    float scaleX   = 1.0f;   // 縦中横の圧縮など
    float scaleY   = 1.0f;
};
```

角度は**数学慣習（y-up）の反時計回りが正**とする。描画先は y-down なので
画面上は逆回りに見え、-90° で欧文の天が右を向く（首を右に傾けて読む向き）。
描画側は「フェイク幅／斜体 → `scaleX`/`scaleY` → `rotation`」の順に合成する。

`GlyphRenderer` は既に全変形を 1 つのアフィン行列へ畳み込んでいるため、
回転はその行列に乗るだけで済む。マスクキャッシュのキーは 2x2 成分を含むので、
**キャッシュ設計の変更は不要**。

---

## 5. インライン要素

| 要素 | 方針 |
|---|---|
| ルビ | 既存のルビ実装（`設計.md 7`）を縦組み座標系へ拡張。モノルビ／グループルビ／熟語ルビ、親文字からのはみ出しと前後文字への掛かりを JLReq に従わせる |
| 縦中横 (TCY) | 2〜3 桁の半角数字等を 1em 角に収めて正立させる。`scaleX` で圧縮 |
| 圏点 | 親文字の右側（縦組み）に別グリフとして配置 |
| 割注 | 行内に 2 行の子ブロックを組む。入れ子の `VerticalLayout` |
| 字取り | 指定 em 数に対してグルーを均等配分 |
| 欧文 | 既定は横倒し（`rotation = -90°`）。`text-orientation: upright` 相当は個別指定 |

---

## 6. ブロック組版

TLF（Apache-2.0）の `ContainerController` のモデルを参考にする。

- 連結された複数コンテナへの流し込み
- 段組（段数・段間）
- ページ分割と、段／ページをまたぐ段落の継続
- インライン画像と回り込み

台本 PDF のページ送り・2 段組（名前欄↔本文列）はこの層で表現する。

---

## 7. PDF 出力

richtext の任意ターゲット（CMake オプション）として追加する。
吉里吉里プラグイン側はリンクしない。

既存 PDF ライブラリに「文字列」を渡す方式は採らない。ライブラリ側が
再シェイピングして組版結果が崩れるため。**グリフ ID を直接書く。**

- `Identity-H` + CIDFontType0 / CIDFontType2 でフォント埋め込み
- 1 グリフずつ `Td` / `Tm` で配置。縦組みでも `Identity-V` は使わない
  （縦メトリクスは組版層で計算済みのため）
- 横倒しが必要なグリフは `Tm` に回転行列を入れる
- 初期実装はサブセット化なしの full embed（台本用途では許容範囲）

---

## 8. フェーズ計画

| Phase | 内容 | 完了条件 |
|---|---|---|
| 1 ✅ | 縦組みプリミティブ：`WritingMode` / `VerticalShaper`（TTB シェイピング・縦メトリクス合成）／`GlyphInfo` 変換フィールド／`GlyphRenderer` 回転対応／1 行の縦配置 | 縦 1 行がベタ組みで正しく描ける。欧文横倒しが出る |
| 2 | **JLReq コア**：`CharClass` / `SpacingTable` / `LineItem` / 自前 `LineBreaker`（Greedy → Knuth–Plass）／禁則・追い込み・追い出し・ぶら下げ | 約物が詰まり、禁則が効き、両端が揃う |
| 3 | インライン要素：ルビの縦対応・縦中横・圏点・割注・字取り | 台本と小説本文が組める |
| 4 | ブロック組版：段組・連結コンテナ流し込み・ページ分割 | 複数ページの流し込みが通る |
| 5 | PDF backend | 同一組版結果から画面と PDF が出る |

Phase 1 完了時点でゲーム画面の縦書きは動き始める。

Phase 1 は実装済み（`sample_vertical` / `output_vertical.bmp` で確認）。
実際に作ったファイルと積み残しは `実装.md` の「縦組みフェーズ 1」を見ること。

---

## 9. ライセンス

| 依存／参照先 | ライセンス | 扱い |
|---|---|---|
| minikin | Apache-2.0 | 既存 submodule |
| HarfBuzz | MIT | minikin 内包 |
| ICU | Unicode License | minikin 内包 |
| FreeType | FTL (BSD-like) | vcpkg |
| Adobe TLF | Apache-2.0 | データモデル・流し込み API の参考。参照する場合は NOTICE を同梱 |
| pTeX / luatex-ja | BSD | JFM モデルの参考 |
| Blink LayoutNG | BSD | writing-mode 実装判断の参考 |
| Vivliostyle | **AGPL-3.0** | **参照しない** |

richtext 本体は MIT を維持する。

---

## 10. 未決事項

- 文字クラス表・アキ量表を外部データ（JSON / CSV）にするか、コード内テーブルにするか
- 縦組み時の `StyledLayout` / `TagParser` の書字方向指定タグの書式（`タグ仕様.md` へ追記）
- Knuth–Plass の実装を Phase 2 に含めるか Phase 4 以降に回すか
