# 実装進捗

## ビルド方法

```bash
# 事前準備: VCPKG_ROOT 環境変数を設定
# テスト用フォントのダウンロード
make fontdata

# CMake 構成（初回または CMakeLists.txt 変更時）
make prebuild                      # OS 自動検出
make prebuild PRESET=x64-windows   # プリセット明示指定

# ビルド
make build
make build BUILD_TYPE=Debug        # デバッグビルド

# サンプル実行（プロジェクトルートから）
./build/x64-windows/Release/sample_render.exe
# → output.bmp が生成される
```

---

## フェーズ 1: フォント管理レイヤー — 完了

- FontFace.cpp — FreeType/minikin MinikinFont 実装
- FontManager.cpp — シングルトンフォントレジストリ
- TextStyle.cpp — MinikinPaint 設定
- Appearance.cpp — DrawStyle 管理

### フォントローダー — 実装済み

ライブラリはファイルシステムに直接アクセスせず、利用側がローダー関数を登録する。

- `setFontDataLoader()` — バッファ方式。`shared_ptr<vector<uint8_t>>` を返す関数を登録。**優先使用**
- `setFontStreamLoader()` — ストリーム方式。`FT_Stream` を返す関数を登録。バッファローダーのフォールバック

`registerFont(fileName, name)` / `registerVariableFont(fileName, name, weight)` でフォント登録時にローダーが呼び出される。`fileName` はローダーに渡される識別子（ファイル名やパス）、`name` はライブラリ内部の登録名。

サンプルでは `std::ifstream` でファイル読み込みするバッファローダーを登録し、`./data/` パスの解決はローダー内で行う。

### フェイス名解決 — 実装済み

`FontFace` は構築時に `FT_Face::family_name` / `style_name` を取り込み、`getFamilyName()` / `getStyleName()` で参照できる。

`FontManager::getFont()` / `createCollection()` は、登録名で完全一致しなかった場合、フォントファイル内の family 単独または `"family Style"` 連結名でフォールバック検索する。これにより `registerFont` 時の登録名と無関係に、フォントファイル自体のフェイス名（CSS 風 family 指定）でフォントを引ける。`createCollection` で family 単独一致した場合、同名 family を持つ全登録エントリが 1 つの `minikin::FontFamily` に集約される。

## フェーズ 2: レイアウト層 — 完了

- TextLayout.cpp — 1行テキストレイアウト（minikin::Layout）
- ParagraphLayout.cpp — 複数行レイアウト（minikin::MeasuredText, breakIntoLines）
  - `getRunWidth()` — MeasuredText ベースの幅計算を追加済み

## フェーズ 3: 描画層 — 完了

- GlyphRenderer.cpp — グリフ描画（カバレッジマスク合成）
- TextRenderer.cpp — テキスト全体描画
  - セグメント配置に `ParagraphLayout::getRunWidth()` を使用し、行分割とレンダリングの幅一致を保証
- StyledLayout.cpp — タグ付きテキストのレイアウト分離
  - `drawStyledText()` のレイアウト処理（タグ解析・行分割・セグメント構築）を `StyledLayout` クラスに分離
  - `TextRenderer::drawStyledLayout()` で事前計算済みレイアウトの描画（maxGlyphs 逐次表示対応）
  - `TextureAtlas::addStyledLayout()` / `getCopyRects(StyledLayout)` でアトラス対応

## フェーズ 4: タグパーサー — 完了

- TagParser.cpp — インラインタグ解析

修正済みの問題:
- ネストされたタグによるスパン重複描画の解消（stable_sort + カーソルベース重複除去）
- 縁取り色の継承（applyColorTag が親の Stroke を引き継ぐよう修正）
- 描画順序の整理（影Fill → Stroke → メインFill の順を保証、`rebuildDrawOrder()` で正規化）
- スタイルスタック空時のデフォルト復帰処理

## フェーズ 5: サンプル実行ファイル — 完了

- sample/sample_render.cpp — BMP ファイル出力サンプル
- sample/sample_sequential.cpp — 逐次表示サンプル（ParagraphLayout + StyledLayout）
- sample/sample_texture.cpp — テクスチャアトラスサンプル（ParagraphLayout + StyledLayout）
- sample/sample_vertical.cpp — 縦組みサンプル（ベタ組み・JLReq 組版・インライン要素）
- sample/sample_vertical_page.cpp — 縦組みのブロック組版サンプル（段組・ページ流し込み）

サンプル内容:
1. 基本テキスト描画
2. 日本語テキスト
3. スタイル変更（サイズ・色・太字）
4. 影付きテキスト
5. パラグラフ描画
6. タグパーサー
7. 大小混在テキスト
8. 上付き・下付き文字
9. 縁取り・影の組み合わせ（通常/縁取りのみ/影のみ/両方/切替）
10–13. CJK 長文折り返し（日本語・韓国語・中国語・アラビア語）
14. CJK 字形比較（日本語・繁体字・簡体字のロケール別字形）
15. フォントサイズバリエーション
16. アラインメント比較（左/中央/右）
17. タグによるサイズ混在パラグラフ
18. CJK 3言語+絵文字混在
19. ルビ（振り仮名）
20. アラインメント比較（左/中央/右）
25. Variable Font ウェイト比較
26. Fake Bold 比較
27. Italic 比較（Fake Italic vs VF Italic）
28. Width 比較（VF wdth 軸 vs Fake Width）

---

# 縦組み（`縦組み設計.md`）

横組み経路（TextLayout / ParagraphLayout / StyledLayout）はそのまま残し、
その隣に縦組み経路を追加していく。フェーズ番号は `縦組み設計.md` の 8 章に対応。

## 縦組みフェーズ 1: 縦組みプリミティブ — 完了

- include/richtext/vertical/WritingMode.hpp, src/vertical/WritingMode.cpp
  - `WritingMode` / `TextOrientation` / `CharOrientation`
  - 縦組みの座標系（u = 縦ベースラインからの左右、v = 行頭からの送り）を定義
  - UAX #50 Vertical_Orientation 相当の判定表（正立の範囲を持ち、それ以外は横倒し）
- include/richtext/vertical/VerticalShaper.hpp, src/vertical/VerticalShaper.cpp
  - `FontCollection::itemize()` でフォントフォールバックを解決し、各ランを
    HarfBuzz に直接シェイピングさせる（minikin::Layout は TTB を扱えないため通さない）
  - 正立ラン → `HB_DIRECTION_TTB`。vert / vrt2（縦字形置換）、vmtx / VORG
    （縦アドバンスと原点）、vkrn / vpal が HarfBuzz 側で効く
  - 横倒しラン → `HB_DIRECTION_LTR` で組んでから列へ 90 度倒す。欧文ベースラインは
    アセント／ディセントの中点が列の中心に来るようずらす
  - `hb_font_create_sub_font()` の font funcs は差し替えない（minikin は
    Skia 相当の funcs を挿すが、それをすると縦メトリクスが取れなくなる）
- include/richtext/vertical/VerticalLayout.hpp, src/vertical/VerticalLayout.cpp
  - 縦 1 行のレイアウト結果。横組みの TextLayout に対応する
- `GlyphInfo` に `rotation` / `scaleX` / `scaleY` を追加
  - 角度は数学慣習（y-up）の反時計回りが正。欧文横倒しは -90°
- `GlyphRenderer::renderGlyph()` が上記を 1 つのアフィン行列へ畳み込む
  （順序は「フェイク幅／斜体 → scaleX/scaleY → rotation」）。マスクキャッシュの
  キーは行列の 2x2 成分を含むのでキャッシュ設計の変更は不要だった
- `GlyphRenderer::renderGlyphs()` — グリフ列を直接描画（縦横共通）
- `TextRenderer::drawVerticalText()` / `drawVerticalLayout()`

確認済み（sample_vertical → output_vertical.bmp）:

- 和文のベタ組みが 1em 送りで揃う
- `vert` による縦字形置換（`「」`・`。、`・長音記号・波ダッシュ）が効く
- 欧文・数字が横倒しで出る（`TextOrientation::Mixed`）
- `Upright` / `Sideways` の全体指定が効く

積み残し:

- 行の幅は今のところ実際の字面の張り出しを返す。列幅を版面から与える形は Phase 4
- 横倒しランのスクリプト分割は `hb_buffer_guess_segment_properties()` 任せ
  （向きで分割済みなのでランはほぼ単一スクリプトになるが、厳密ではない）
- 横倒しのカラー絵文字ビットマップは回転しない（`renderBitmap` はアフィンを
  受け取るが `GlyphInfo::rotation` を渡していない）

## 縦組みフェーズ 2: JLReq コア — 完了

和文組版を「グリフ配置の問題」ではなく「文字クラス間のアキ（グルー）と
ペナルティの列を解く問題」として扱う（縦組み設計.md 1.1 / 4.2）。

- include/richtext/vertical/CharClass.hpp, src/vertical/CharClass.cpp
  - JLReq 附属書 A の文字クラス（始め括弧類・終わり括弧類・句点類・読点類・
    中点類・区切り約物・分離禁止文字・繰返し記号・長音記号・小書きの仮名・
    前置／後置省略記号・漢字等・仮名・欧文用文字 …）
  - 仮想ボディ内での字面の寄り（`BodyAlign`）と、行頭禁則／行末禁則の判定
- include/richtext/vertical/SpacingTable.hpp, src/vertical/SpacingTable.cpp
  - JLReq 表 3 のアキ量を「自然値・伸び・縮み」（`GlueSpec`、em 単位）で返す
  - N×N の表ではなく判定の順序で表現している（元の規定と突き合わせやすい形）
- include/richtext/vertical/LineItem.hpp
  - Box / Glue / Penalty。合法ブレーク点の規則は TeX と同じで
    「Penalty で ∞ 未満」または「Glue で直前が Box」
  - 禁則は Glue の直前に Penalty(∞) を挟んで表す。ぶら下げは「幅が負の Penalty」
- include/richtext/vertical/LineItemBuilder.hpp, src/vertical/LineItemBuilder.cpp
  - シェイピング結果（`ShapedCluster` 列）→ アイテム列
  - 約物の仮想ボディを半角へ詰め、差分を `LineItem::glyphOffset` に逃がす
  - 欧文間隔は Box ではなく Glue にして、そこを欧文の唯一の切れ目にする
- include/richtext/vertical/LineBreaker.hpp, src/vertical/LineBreaker.cpp
  - Greedy（既定）と Knuth–Plass（total-fit）。minikin の LineBreaker は
    伸縮グルーを扱えないので使わない
  - 行が確定すると調整比 `ratio` が決まり、追い込み・追い出し・両端揃えが
    「グルーの伸縮でどう行長に合わせるか」の 1 つの問題に統一される
- include/richtext/vertical/VerticalParagraphLayout.hpp / .cpp
  - 改行で段落へ分け、上記を通して行ごとの `GlyphInfo` 列を作る
  - `getLinePosition()` が vertical-rl / vertical-lr の列送りを返す
- `VerticalShaper::Result` に `ShapedCluster` 列を追加（組版層はクラスタ単位で扱う）
- `TextRenderer::drawVerticalParagraph()` / `drawVerticalParagraphLayout()`

確認済み（sample_vertical → output_vertical.bmp）:

- ベタ組みと JLReq 組版の対比。句読点・括弧が半角に詰まり、和欧間に四分アキが入る
- 行頭禁則・行末禁則の違反 0（サンプルが毎回検算して件数を出す）
- 両端揃え: 全行が指定行長ちょうど（グルーの縮みによる追い込み）
- ぶら下げ: 同じ文と行長で 7 行 → 6 行になり、句読点が版面外へ出る
- Greedy と Knuth–Plass の双方が同じ結果に収束する（この本文では差が出ない長さ）

積み残し:

- 版面から列幅を与える形、段組・ページ分割 → Phase 4
- 文字クラス表・アキ量表は今のところコード内テーブル
  （外部データ化は `縦組み設計.md` 10 章の未決事項のまま）
- `StyledLayout` / `TagParser` からの縦組み指定（書字方向タグ）は未対応。
  縦組み経路は単一スタイルのみ

## 縦組みフェーズ 3: インライン要素 — 完了

ルビ・縦中横・圏点・割注・字取りを、**描画時の後処理ではなく組版アイテムへ
落とし込む**形で実装した。こうしないと行分割・約物の詰め・両端揃えと矛盾する
（ルビが親文字を広げる、縦中横が 1em の Box になる、といったことが行長に効く）。

- include/richtext/vertical/InlineAnnotation.hpp, src/vertical/InlineAnnotation.cpp
  - 本文の文字範囲に対する注記（`AnnotationType` / `RubyMode` / `EmphasisMark`）
  - 生成ヘルパー（`InlineAnnotation::ruby()` / `tateChuYoko()` / `emphasis()` /
    `warichu()` / `jidori()`）
- `buildLineItems()` に注記の解決を追加（`src/vertical/LineItemBuilder.cpp`）
  - **ルビ** — 親より短ければ中付きで均等配分。長ければ JLReq に従って
    まず前後の文字へ最大 1 ルビ文字分まで掛け、足りない分だけ親文字列を
    「前後 1 : 文字間 2」の比で広げる。掛けてよいのは仮名・漢字のみ。
    グループルビは範囲内を分割禁止にする。モノルビは `|` 区切りで
    親文字 1 文字ずつに対応させる
  - **縦中横** — 範囲を minikin の `TextLayout` で横組みに組み、1em に
    入らない分だけ `GlyphInfo::scaleX` で圧縮して 1em 角の Box にする
  - **圏点** — 親文字の右（u 正方向）に別グリフとして配置。既定は
    ゴマ点 U+FE45 で、親の半分のサイズ
  - **割注** — 内容を半分の文字サイズで組み、`LineBreaker` で 2 行に割って
    1 行幅へ収める。vertical-rl では先に来る段が右側
  - **字取り** — 範囲の総幅と目標 em 数の差を文字間へ均等配分（固定アキ）
- `LineItem` に合成／付随グリフ（`glyphs` / `ownGlyphs`）と u 方向の張り出しを追加
- `GlyphInfo` に `fontSize` を追加。0 なら描画時のスタイルのサイズを使う。
  1 行の中に本文・ルビ・圏点・割注の 4 サイズが混ざるので必要になった
- `VerticalParagraphLayout::layout()` と
  `TextRenderer::drawVerticalParagraph()` に注記付きのオーバーロード

確認済み（sample_vertical → output_vertical.bmp）:

- グループルビ（親より長い例）で親文字列が広がり、前の文字へ掛かる
- モノルビが親文字 1 文字ずつに乗る
- 縦中横の `25` が 1em 角に正立で収まる
- 圏点が親文字の右に出る
- 割注が 2 行で 1 行幅に収まる
- 字取りで 2 文字が 4em に均等割り付けされる
- 横組みサンプル 3 種の出力に変化なし

積み残し:

- 熟語ルビ（グループとモノの中間）は未対応
- ルビが行頭・行末へ来たときの掛かりの抑制が無く、版面外へ出ることがある
- 圏点は右側固定（左側指定なし）
- 割注は 2 行固定。3 行必要な内容は段を伸ばして 2 行に収める
- 注記の入力は API のみ。タグからの指定（`タグ仕様.md`）は未対応

## 縦組みフェーズ 4: ブロック組版 — 完了

- include/richtext/vertical/BlockLayout.hpp, src/vertical/BlockLayout.cpp
  - `BlockContainer` — 版面 1 つ（＝ページや枠）。段数と段間を持つ
  - 連結したコンテナへ段落列を順に流し込む。コンテナは段に分かれ、
    縦組みでは段が上下に並び、段の中で列が右から左（vertical-rl）へ進む
  - 列の位置は「次の列が置かれる側の端」1 つで持つ。行送りが段落ごとに
    違っても、段落後のアキ（`spaceAfter`）を入れても、この値を動かすだけで済む
  - **段の高さ＝行長**なので、高さの違うコンテナへまたぐときは残りのテキストを
    その行長で組み直す（TLF の ContainerController と同じ考え方）。
    組み直した断片は `Chunk` として持ち、`charOffset` で元の段落へ戻せる
  - 入りきらなかった分は `getOverflow()` で「どの段落の何文字目から」が分かる
- `TextRenderer::drawBlockLayout()` — 全コンテナを描画（`maxChars` で逐次表示）
- `TextRenderer::drawBlockContainer()` — コンテナ 1 つ分＝1 ページだけ描画。
  オフセットを渡せるので、ページごとに別のキャンバスへ出せる

確認済み（sample_vertical_page → output_vertical_page.bmp）:

- 3 ページ × 2 段（3 ページ目だけ 1 段）へ 9 段落・52 行を流し込み
- 段をまたぐ継続 3 箇所、行長が変わったことによる組み直し 1 箇所
- 段落は必ず新しい列から始まる
- あふれ無し（`getOverflow().has == false`）

積み残し:

- インライン画像と回り込みは未実装（`縦組み設計.md` 6 章）。
  Phase 4 の完了条件「複数ページの流し込み」に含まれないため後回しにした
- 段抜き（見出しが段をまたぐ）、段の途中で始める指定は未対応
- 行数を段ごとに揃える（バランス取り）は未対応

---

## 吉里吉里プラグイン

TJS バインディング（main.cpp）は `../krkr_richtext` に分離。

---

## 既知の課題

### フェイク斜体 — 実装済み

`GlyphRenderer` が `FontFakery::isFakeItalic()` を参照し、`renderPath` でグリフパスにシアー変換（skewX = -0.25、Android と同じ約14度）を適用する。パスキャッシュはシアー前の形状を保持し、描画時に変換するため追加のキャッシュ消費はない。

### フェイク太字 — 実装済み

`GlyphRenderer` が `FontFakery::isFakeBold()` を参照し、グリフパスに同色ストローク（フォントサイズの 1/24 幅）を重ねることで太字をシミュレートする。minikin は要求ウェイト ≥ 600 かつ実際のウェイトとの差 ≥ 200 の場合に自動的にフェイクボールドフラグを立てる。

### バリアブルフォント — 実装済み

`FontFace` が FreeType の `FT_Get_MM_Var` でバリアブルフォントの軸情報を取得し、`setVariations()` で `FT_Set_Var_Design_Coordinates` を呼び出して軸値を設定する。`FontManager::registerVariableFont()` で同じフォントファイルから異なるウェイト・スタイルの FontFace を複数作成し、minikin の FontFamily に登録。

**対応軸:**

- **wght（ウェイト）**: `fontWeight` 指定で minikin が自動選択。未対応時はフェイクボールド
- **ital（イタリック）**: `italic=true` + `registerVariableFont(path, name, w, true)` で Italic FontFace を登録。未対応時はフェイクイタリック（skewX=-0.25）
- **wdth（字幅）**: `fontWidth` 指定。`GetHorizontalAdvance` 内で wdth 軸を設定し、軸でカバーできない分は fakeScaleX で補う。描画時も同様に `GlyphRenderer` が wdth 軸 + fakeScaleX を適用

**minikin 修正:**

- `MinikinPaint` に `fontWidth` フィールドを追加（`LayoutCacheKey` にも反映）
- `FontFace::GetHorizontalAdvance` で `paint.fontWidth` を参照し、wdth 軸設定 + fake スケールを適用

**タグ対応:**

- `<font width="75">` で fontWidth を指定可能
